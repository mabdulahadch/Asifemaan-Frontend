// Backward compatibility wrapper — delegates to LanguageContext
// Components importing from here will continue to work
export { useScript, LanguageProvider as ScriptProvider } from "./LanguageContext";
